@model IEnumerable<OnlineMovieBooking.Models.ShowSeat>

@{
    ViewBag.Title = "Index";
}

<h2>Show Seats List</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Booking.Status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CinemaSeat.SeatNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Show.ShowId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Booking.Status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CinemaSeat.SeatNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Show.ShowId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Price)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ShowSeatId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ShowSeatId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ShowSeatId })
        </td>
    </tr>
}

</table>
