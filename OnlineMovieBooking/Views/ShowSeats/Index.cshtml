@model IEnumerable<OnlineMovieBooking.ViewModels.ShowSeatViewModel>

@{
    ViewBag.Title = "Index";
}
@Html.Partial("PartialAdminPanel")
<h2>Show Seats List</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ShowId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ShowId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ShowSeatId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ShowSeatId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ShowSeatId })
        </td>
    </tr>
}

</table>
