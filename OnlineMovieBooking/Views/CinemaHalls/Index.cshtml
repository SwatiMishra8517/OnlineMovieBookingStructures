@model IEnumerable<OnlineMovieBooking.ViewModels.CinemaHallViewModel>

@{
    ViewBag.Title = "Index";
}
@Html.Partial("PartialAdminPanel")
<h2>Cinema Hall List</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CinemaHallId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CinemaHallId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CinemaHallId })
        </td>
    </tr>
}

</table>
